# Course Registration API

This is a web application built with ASP.NET course that provides a RESTful API backend for a course registration system.

To run this application, run the following commands:
```shell
cd CourseRegistration.Api
dotnet run
```