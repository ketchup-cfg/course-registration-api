# course-registration-api

This is a web application built with Spring Boot that provides RESTful API endpoints that can be used to manage data for a course registration system.

To run the application, just run `mvn spring-boot:run` from the root of the application.
